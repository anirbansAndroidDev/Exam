<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <style name="MyTabTheme" parent="@android:style/Theme.Light">
        <item name="android:tabWidgetStyle">@style/MyLightTabWidget</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="MyLightTabWidget" parent="@android:style/Widget.TabWidget">
        <!-- set textColor to red, so you can verify that it applied. -->
        <!-- <item name="android:textColor">#f00</item>  -->
        <item name="android:textSize">20px</item>
        <item name="android:textColor">#f00</item>
    </style>

</resources> 